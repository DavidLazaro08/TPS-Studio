<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.canvas.Canvas?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.ComboBox?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.ScrollPane?>
<?import javafx.scene.control.Separator?>
<?import javafx.scene.control.ToggleButton?>
<?import javafx.scene.control.ToggleGroup?>
<?import javafx.scene.control.Tooltip?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.Region?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.layout.VBox?>

<!--
    Layout raíz: StackPane con dos capas.
    Capa 1 (abajo): BorderPane con toolbar + paneles laterales + canvas.
    Capa 2 (encima): rightPanel como overlay alineado TOP_RIGHT con margen superior
               igual a la altura de la toolbar (~46px), para que nunca la solape.
    Ventaja: el canvas NO cambia de tamaño al abrir/cerrar el panel; en su lugar
             el canvas se desplaza suavemente con TranslateTransition (adjustCanvasCentering).
-->
<StackPane xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1"
           fx:controller="com.tpsstudio.view.controllers.MainViewController"
           styleClass="root">

    <!-- CAPA 1: Layout principal -->
    <BorderPane fx:id="rootPane">

        <!-- BARRA SUPERIOR -->
        <top>
            <VBox>
                <HBox alignment="CENTER_LEFT" styleClass="main-toolbar" spacing="12">
                    <padding>
                        <Insets top="10" bottom="10" left="15" right="15" />
                    </padding>

                    <!-- GRUPO 1: Selector de Modo -->
                    <HBox alignment="CENTER_LEFT" styleClass="toolbar-group">
                        <fx:define>
                            <ToggleGroup fx:id="modeToggleGroup" />
                        </fx:define>
                        <ToggleButton fx:id="btnModeExport" text="Producción" toggleGroup="$modeToggleGroup"
                                      onAction="#onModeExport" styleClass="mode-toggle-left" selected="true" prefHeight="30" />
                        <ToggleButton fx:id="btnModeEdit" text="Diseño" toggleGroup="$modeToggleGroup"
                                      onAction="#onModeEdit" styleClass="mode-toggle-right" prefHeight="30" />
                    </HBox>

                    <Region styleClass="subtle-separator" />

                    <!-- GRUPO 2: Acciones de Archivo -->
                    <HBox spacing="2" alignment="CENTER_LEFT" styleClass="toolbar-group">
                        <Button fx:id="btnNuevo" text="📄" onAction="#onNuevoProyecto" styleClass="icon-btn" prefHeight="30">
                            <tooltip><Tooltip text="Nuevo Proyecto" /></tooltip>
                        </Button>
                        <Button fx:id="btnAbrir" text="📂" onAction="#onAbrirProyecto" styleClass="icon-btn" prefHeight="30">
                            <tooltip><Tooltip text="Abrir Proyecto" /></tooltip>
                        </Button>
                        <Button fx:id="btnGuardar" text="💾" onAction="#onGuardarProyecto" styleClass="icon-btn" prefHeight="30">
                            <tooltip><Tooltip text="Guardar Cambios" /></tooltip>
                        </Button>
                    </HBox>

                    <Region styleClass="subtle-separator" />

                    <!-- GRUPO 3: Salida -->
                    <HBox spacing="6" alignment="CENTER_LEFT" styleClass="toolbar-group">
                        <Button fx:id="btnExportar" text="📤 PDF" onAction="#onExportarProyecto" styleClass="action-btn" prefHeight="30">
                            <tooltip><Tooltip text="Exportar a PDF" /></tooltip>
                        </Button>
                        <Button fx:id="btnImprimir" text="🖨 Imprimir" onAction="#onImprimirProyecto" styleClass="action-btn-primary" prefHeight="30">
                            <tooltip><Tooltip text="Imprimir Tarjeta" /></tooltip>
                        </Button>
                    </HBox>

                    <Region styleClass="subtle-separator" />

                    <!-- GRUPO 4: Herramientas -->
                    <HBox spacing="10" alignment="CENTER_LEFT" styleClass="toolbar-group">
                        <HBox alignment="CENTER" spacing="0" styleClass="integrated-zoom" prefHeight="30">
                            <Button text="-" onAction="#onZoomOut" styleClass="zoom-btn-minimal" prefHeight="30" />
                            <Label fx:id="lblZoom" text="100%" styleClass="zoom-label-fixed" prefHeight="30" />
                            <Button text="+" onAction="#onZoomIn" styleClass="zoom-btn-minimal" prefHeight="30" />
                        </HBox>
                        
                        <Region styleClass="micro-separator" />
                        
                        <ToggleButton fx:id="toggleFrenteDorso" text="Frente" onAction="#onToggleFrenteDorso"
                                      styleClass="view-toggle" selected="true" prefHeight="30" minWidth="60" />
                        <ToggleButton fx:id="toggleGuias" text="Guías" onAction="#onToggleGuias"
                                      styleClass="view-toggle" selected="true" prefHeight="30" minWidth="55" />
                        
                        <HBox alignment="CENTER_LEFT" spacing="6" styleClass="troquel-group">
                            <Label text="Troquel" styleClass="control-label" />
                            <ComboBox fx:id="cmbTroquelToolbar" promptText="Sin troquel" prefWidth="130" prefHeight="30"
                                      styleClass="toolbar-combo" />
                        </HBox>
                    </HBox>

                    <Region HBox.hgrow="ALWAYS" />

                    <!-- GRUPO 5: Paneles -->
                    <HBox fx:id="bloqueContextual" spacing="6" alignment="CENTER_LEFT" visible="false" managed="false">
                        <Region styleClass="subtle-separator" />
                        <ToggleButton fx:id="togglePropiedades" text="⚙ Propiedades" onAction="#onTogglePropiedades"
                                      styleClass="panel-toggle" prefHeight="30" />
                        <ToggleButton fx:id="toggleDatosVariables" text="📊 Datos" onAction="#onToggleDatosVariables"
                                      styleClass="panel-toggle" prefHeight="30" />
                    </HBox>
                </HBox>
            </VBox>
        </top>

        <left>
            <VBox styleClass="side-panel" prefWidth="240" minWidth="200" maxWidth="300">
                <!-- Zona superior: Proyectos / Herramientas (ahora sin scroll global) -->
                <VBox fx:id="leftPanel" VBox.vgrow="ALWAYS" />
                
                <VBox spacing="0" alignment="BOTTOM_CENTER" VBox.vgrow="NEVER">
                    <Region style="-fx-background-color: #2a2628; -fx-min-height: 1; -fx-max-height: 1;" />
                </VBox>
                
                <!-- Zona inferior: Perfil de Usuario -->
                <VBox fx:id="userProfileArea" spacing="4" alignment="CENTER_LEFT" styleClass="user-profile-panel">
                    <padding>
                        <Insets top="12" bottom="12" left="15" right="15" />
                    </padding>
                    <HBox spacing="8" alignment="CENTER_LEFT">
                        <Label text="👤" style="-fx-opacity: 0.5;" />
                        <Label fx:id="lblCurrentUser" text="Admin" styleClass="user-profile-name" />
                    </HBox>
                    <Button fx:id="btnLogout" text="Cerrar sesión" onAction="#onLogout" styleClass="logout-btn" />
                </VBox>
            </VBox>
        </left>

        <!-- ÁREA DE TRABAJO (Canvas) -->
        <center>
            <StackPane fx:id="canvasContainer" styleClass="canvas-area">
                <Canvas fx:id="canvas" width="750" height="600" />
            </StackPane>
        </center>

    </BorderPane>

    <!-- CAPA 2: Panel derecho como overlay — empieza bajo la toolbar (margen 46px) -->
    <VBox fx:id="rightPanel" styleClass="side-panel"
          prefWidth="290" minWidth="290" maxWidth="290"
          visible="false" managed="false">
        <StackPane.alignment>TOP_RIGHT</StackPane.alignment>
        <StackPane.margin>
            <Insets top="46" />
        </StackPane.margin>
    </VBox>

</StackPane>
